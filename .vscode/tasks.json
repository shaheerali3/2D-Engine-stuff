{
    "version": "2.0.0",
    "tasks": 
    [
        {
            "label": "build",
            "args": ["code/game.c"],
            "command" : "${workspaceFolder}/build.bat",
            "options": {

                "cwd": "${workspaceFolder}/"
            },

            "type": "shell",
            "group":
            {
                "kind": "build",
                "isDefault": true
            }

        }

    ]

    
}